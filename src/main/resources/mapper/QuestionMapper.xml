<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.maomao.zhihu.mapper.QuestionMapper">

    <resultMap id="BaseResultMap" type="com.maomao.zhihu.entity.Question">
            <id property="id" column="id" jdbcType="BIGINT"/>
            <result property="title" column="title" jdbcType="VARCHAR"/>
            <result property="introduce" column="introduce" jdbcType="VARCHAR"/>
            <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
            <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
            <result property="isDelete" column="is_delete" jdbcType="BIT"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,title,introduce,
        create_time,update_time,is_delete
    </sql>

    <resultMap id="QuestionAnswerResultMap" type="com.maomao.zhihu.entity.Question">
        <id property="id" column="id" jdbcType="BIGINT"/>
        <result property="title" column="title" jdbcType="VARCHAR"/>
        <result property="introduce" column="introduce" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
        <result property="isDelete" column="is_delete" jdbcType="BIT"/>
        <association property="user"
                     select="com.maomao.zhihu.mapper.UserMapper.getUserById"
                     column="user_id">
        </association>
        <collection property="answers" ofType="com.maomao.zhihu.entity.Answer">
            <id property="id" column="answer_id" jdbcType="BIGINT"/>
            <result property="content" column="content" jdbcType="VARCHAR"/>
            <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
            <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
            <result property="isDelete" column="is_delete" jdbcType="BIT"/>
            <association property="user"
                         select="com.maomao.zhihu.mapper.UserMapper.getUserById"
                         column="user_id">
            </association>
        </collection>
    </resultMap>
    <select id="getQuestionById" resultMap="QuestionAnswerResultMap">
        SELECT a.id,a.title,a.introduce,a.create_time,a.update_time,a.is_delete,a.user_id,
        b.id answer_id,b.content,b.create_time,b.update_time,b.is_delete
        FROM (SELECT * FROM question a LEFT JOIN question_answer b ON a.id = b.question_id where a.is_delete = 0) a
        LEFT JOIN answer b
        ON a.answer_id = b.id
        where a.id = #{id}
    </select>
    <select id="getManyQuestion" resultMap="QuestionAnswerResultMap">
        SELECT a.id,a.title,a.introduce,a.create_time,a.update_time,a.is_delete,a.user_id,
        b.id answer_id,b.content,b.create_time,b.update_time,b.is_delete
        FROM (SELECT * FROM question a LEFT JOIN question_answer b ON a.id = b.question_id where a.is_delete = 0) a
        LEFT JOIN answer b
        ON a.answer_id = b.id
    </select>
</mapper>
